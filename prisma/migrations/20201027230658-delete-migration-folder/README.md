# Migration `20201027230658-delete-migration-folder`

This migration has been generated by Yoni at 10/27/2020, 4:06:58 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
"id" text   NOT NULL ,
"userName" text   ,
"email" text   ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Wiggle" (
"id" SERIAL,
"schedule" text   NOT NULL ,
"userId" text   NOT NULL ,
"contactId" integer   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Contact" (
"id" SERIAL,
"name" text   ,
"phoneNumber" text   NOT NULL ,
PRIMARY KEY ("id")
)

ALTER TABLE "public"."Wiggle" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Wiggle" ADD FOREIGN KEY ("contactId")REFERENCES "public"."Contact"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201027230658-delete-migration-folder
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,37 @@
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider        = "prisma-client-js"
+  previewFeatures = ["connectOrCreate"]
+}
+
+model User {
+  id       String   @id
+  userName String?
+  email    String?
+  wiggles  Wiggle[]
+
+}
+
+model Wiggle {
+  id   Int  @id @default(autoincrement())
+  user User @relation(fields: [userId], references: [id])
+
+  schedule String // change to enum once all permutations are determined
+  // add message type
+  contact  Contact @relation(fields: [contactId], references: [id])
+
+  userId    String
+  contactId Int
+}
+
+model Contact {
+  id          Int     @id @default(autoincrement())
+  name        String?
+  phoneNumber String
+
+  Wiggle Wiggle[]
+}
```


