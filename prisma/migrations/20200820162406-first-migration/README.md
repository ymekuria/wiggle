# Migration `20200820162406-first-migration`

This migration has been generated by Yoni at 8/20/2020, 4:24:06 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
"id" SERIAL,
"userName" text  NOT NULL ,
PRIMARY KEY ("id"))

CREATE TABLE "public"."Wiggle" (
"id" SERIAL,
"schedule" text  NOT NULL ,
"userId" integer  NOT NULL ,
"contactId" integer  NOT NULL ,
PRIMARY KEY ("id"))

CREATE TABLE "public"."Contact" (
"id" SERIAL,
"name" text   ,
"phoneNumber" text  NOT NULL ,
PRIMARY KEY ("id"))

CREATE UNIQUE INDEX "User.userName" ON "public"."User"("userName")

ALTER TABLE "public"."Wiggle" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Wiggle" ADD FOREIGN KEY ("contactId")REFERENCES "public"."Contact"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200820162406-first-migration
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,37 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id       Int      @id @default(autoincrement())
+  userName String   @unique
+  wiggles  Wiggle[]
+
+}
+
+model Wiggle {
+  id   Int  @id @default(autoincrement())
+  user User @relation(fields: [userId], references: [id])
+
+  schedule  String // change to enum once all permutations are determined
+  // add message type
+  contact   Contact @relation(fields: [contactId], references: [id])
+  userId    Int
+  contactId Int
+}
+
+model Contact {
+  id          Int     @id @default(autoincrement())
+  name        String?
+  phoneNumber String
+
+  Wiggle Wiggle[]
+}
```


